const Header = () => {
  return (
    <header className="absolute top-0 left-0 right-0 bg-gradient-to-b from-black/80 via-black/70 to-transparent text-white py-2 z-50 text-center">
      <h1 className="text-2xl font-semibold">Flight Tracker</h1>
    </header>
  );
};

export default Header;
